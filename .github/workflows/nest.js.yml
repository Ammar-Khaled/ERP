name: Nest.js CI

on:
  push:
    branches: [ "main" ]


jobs:
  run-dev:

    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v4
    - run: npm i
    - run: cd backend && npm run start:dev
